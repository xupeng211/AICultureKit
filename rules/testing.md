# 测试编写与覆盖率要求

## 测试框架
- 使用 `pytest`。
- 测试文件放在 `tests/` 目录，命名为 `test_*.py`。

## 编写规范
- 每个核心模块必须至少有 1 个测试文件。
- 每个公共函数/类必须有单元测试。
- 边界条件、异常处理必须覆盖。

## 覆盖率要求
- 覆盖率 < 80% → 禁止推送。
- 使用 `make coverage` 检查。

## 执行方式
- 本地提交前必须执行：
  ```bash
  make test
  make coverage
  ```
- CI 环境会自动运行测试。 