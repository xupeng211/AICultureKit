---
description: 足球预测系统项目开发规则
globs: ["**/*"]
alwaysApply: true
rules:
  - 本项目必须先读取 docs/AI_PROMPT.md 和 rules.md 的内容再开始工作。
  - 所有模块代码必须放在 src/ 下，测试代码必须放在 tests/ 下。
  - 必须使用 Makefile 中的命令来运行、构建和测试，禁止直接运行脚本。
  - 严禁绕过 pre-commit 检查或 CI 流程。
  - 在生成代码时必须包含详细的中文注释，解释每一步的设计和逻辑。
  - 修改现有功能时优先在原文件中修改，除非确实需要新建文件。
  - 代码提交前必须通过 `make ci-check`，保证格式化、类型检查和测试全部通过。
